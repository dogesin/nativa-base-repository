# habitables - Docker Setup

Project generated by Nativa with Docker support.

## 🚀 Quick Start

### Prerequisites
- Docker Desktop installed and running
- That's it! 🎉

### Start the Project

```bash
# Start all services
docker compose up

# Start in detached mode (background)
docker compose up -d
```

### Access Your Application

- **Frontend (Next.js)**: http://localhost:3000- **Backend API (FastAPI)**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs- **Database**: localhost:5432

## 📋 Available Commands

```bash
# Stop all services
docker compose down

# View logs
docker compose logs -f

# View logs for specific service
docker compose logs -f frontend
docker compose logs -f backend
docker compose logs -f postgres

# Rebuild containers after Dockerfile changes
docker compose up --build

# Remove all containers and volumes (fresh start)
docker compose down -v

# Execute command in running container
docker compose exec frontend sh
docker compose exec backend bash
docker compose exec postgres psql -U postgres -d habitables
```

## 🔧 Environment Variables

All environment variables are in the `.env` file at the root of the project.

**Important**: Never commit `.env` to version control!



## 📁 Project Structure

```
habitables/
├── frontend/              # Next.js application
│   ├── Dockerfile.dev
│   └── ...├── backend/               # FastAPI application
│   ├── Dockerfile.dev
│   └── ...├── postgres/              # PostgreSQL scripts
│   └── init.sql├── docker-compose.yml     # Service orchestration
├── .env                   # Environment variables
└── README.md
```

## 🐛 Troubleshooting

### Port Already in Use

If you get a port conflict error:

```bash
# Find process using the port (example for port 3000)
lsof -i :3000

# Kill the process
kill -9 <PID>
```

Or change the port in `docker-compose.yml`.

### Database Connection Issues

```bash
# Check database is running
docker compose ps

# View database logs
docker compose logs postgres

# Restart database
docker compose restart postgres
```

### Hot Reload Not Working

Make sure volumes are properly mounted in `docker-compose.yml`.

## 📚 Learn More

- [Next.js Documentation](https://nextjs.org/docs)- [FastAPI Documentation](https://fastapi.tiangolo.com/)- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Docker Documentation](https://docs.docker.com/)

---

Generated with ❤️ by Nativa
